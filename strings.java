import java.util.*;
public class strings {
    public static void main(String[] args) {
        Scanner sc=new Scanner(System.in);
        // String name= new String("Lalit");//initialisation of string
        // String name="Lalit";
        String name=sc.next();
        // String name1=sc.next();
        //s1.toLowerCase();
        //s1.toUpperCase();
        //s1.trim();removes spaces from whole string
        //s1.substring(start,end);end is excluded
        //s1.replace('oldchar','newchar'); 
        //s1.startsWith("argument");true/false returned
        //s1.endsWith("argument");true/false returned
        //s1.charAt(index);returns character at index
        //s1.indexOf("str",startindex);returns index of str
        //s1.lastIndexOf("str");returns last index of given string
        //s1.equals("String");//returns true or false
        //Integer.parseInt(string);string to integer
        //String.valueOf(int);//integer to string*+
        System.out.println(name.length());
    }
}
